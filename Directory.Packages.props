<Project>
    <PropertyGroup>
        <CentralPackageTransitivePinningEnabled>true</CentralPackageTransitivePinningEnabled>
        <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>

        <DotNetVersion>10.0.12</DotNetVersion>
    </PropertyGroup>

    <ItemGroup>
        <PackageVersion Include="AwesomeAssertions" Version="9.6.0"/>
        <PackageVersion Include="Azure.Storage.Blobs" Version="12.29.2"/>
        <PackageVersion Include="BenchmarkDotNet" Version="0.15.8"/>
        <PackageVersion Include="CsCheck" Version="4.9.1"/>
        <PackageVersion Include="FluentValidation" Version="12.1.1"/>
        <PackageVersion Include="K4os.Compression.LZ4" Version="1.3.8"/>
        <PackageVersion Include="MemoryPack" Version="1.21.4"/>
        <PackageVersion Include="Microsoft.EntityFrameworkCore.SqlServer" Version="$(DotNetVersion)"/>
        <PackageVersion Include="Microsoft.Extensions.Caching.Memory" Version="$(DotNetVersion)"/>
        <PackageVersion Include="Microsoft.Extensions.Hosting.Abstractions" Version="$(DotNetVersion)"/>
        <PackageVersion Include="Microsoft.Extensions.Logging.Abstractions" Version="$(DotNetVersion)"/>
        <PackageVersion Include="Microsoft.Extensions.Options.ConfigurationExtensions" Version="$(DotNetVersion)"/>
        <PackageVersion Include="Microsoft.Testing.Extensions.CodeCoverage" Version="18.11.2"/>
        <PackageVersion Include="Microsoft.VisualStudio.Threading.Analyzers" Version="18.7.23"/>
        <PackageVersion Include="morelinq" Version="4.4.0"/>
        <PackageVersion Include="Nerdbank.GitVersioning" Version="3.10.94"/>
        <PackageVersion Include="Nerdbank.MessagePack" Version="1.3.86"/>
        <PackageVersion Include="NorthSouthSystems.MessagePackable" Version="1.1.15-beta"/>
        <PackageVersion Include="NorthSouthSystems.Text" Version="8.0.1"/>
        <PackageVersion Include="PolyType" Version="1.4.1"/>
        <PackageVersion Include="Scrutor" Version="7.0.0"/>
        <PackageVersion Include="StackExchange.Redis" Version="3.3.0"/>
        <PackageVersion Include="System.IO.Hashing" Version="$(DotNetVersion)"/>
        <PackageVersion Include="Vogen" Version="8.0.7"/>
        <PackageVersion Include="xunit.v3" Version="4.0.1"/>

        <!-- Transitive Pinning -->
        <!-- This will help prevent large(r) potentially breaking version bumps, and it will also -->
        <!-- allow us to benefit from potential performance improvements to newer package versions. -->
        <!-- In some cases, transitive pinning is necessary due to an unexplicable error, likely related to -->
        <!-- explicit nuget.config package mapping (which we have since relaxed with a catch-all pattern): -->
        <!-- NU1102 "Unable to find package ... with version (>= [a valid version])" -->

        <!-- Microsoft.EntityFrameworkCore.SqlServer -->
        <PackageVersion Include="Microsoft.Data.SqlClient" Version="7.1.0"/>
    </ItemGroup>
</Project>
